# 任务清单：AI 智能排程日历应用（全新构建）

## 一、已完成：核心基建与基础日历交互
- [x] **项目框架**：GoRouter 路由、Riverpod 状态管理、Drift(SQLite) 极速本地存储。
- [x] **视图与交互**：
  - 左侧统一管理日历本/清单目录，右侧自适应滑出「未排程/收集箱」面板。
  - 时间轴（Timeline）：当日所有日程与任务色块渲染，支持左右天数切换与「回到今日」。
  - 周视图与月视图：分别支持七日纵览和整月高能视觉标记。
### 2.1 时间轴高级交互 (Timeline Interactions)
- [x] 拖拽任务改变位置 (改变 `dtstart` 并同步数据库)。
- [x] 底缘拖拽调长 (改变 `durationMinutes` 或 `dtend`)。
- [x] **跨区拖拽编排**：支持从右侧「收集箱」直接拖拽任务到时间轴完成排期。
- [x] **撤回收集箱**：支持将时间轴上的已排期任务重新拖回右侧收集箱（取消排期）。
- [ ] **【重点体验修复】** 修复左侧 DragTarget 的 `globalToLocal` 的 Y 轴偏移与 `scrollOffset` 重复叠加拖拽偏移 Bug（前次尝试方案验证失败，需重新梳理底层拖放边界）。
- [ ] **【高优交互优化】** 任务拖拽逻辑需完全对齐日程事件的丝滑吸附手感：仅在彻底离开时间轴时才触发浮空态，时间轴内部严格依靠手势执行丝滑原位平移。（由 Provider 及 Ghost Block 拼凑的原生 LongPressDraggable 反馈方案已被推翻，待未来以 Overlay 等全局拦截手势方案重构）。
- [x] **云端同步与扩展**：
  - iCalendar (`.ics`) 导入与导出。
  - Outlook Exchange / Microsoft Graph API 双向同步。
- [x] **设置页全量覆盖**：主题切换、工作起止时间、首选提醒、时间制式、起始星期等，并完成全方位数据联动。

---

## 二、已完成：时间管理与自动追踪基础 (Phase 2A & 2B)
- [x] **后台自动感知 (WindowSensor)**：接入 Win32 FFI，每 5 秒获取前台窗口标题与进程名，识别全屏沉浸/游戏状态。
- [x] **本地规则分类 (ActivityClassifier)**：基于关键词将捕获的窗口匹配为编程、娱乐、办公、设计等标签。
- [x] **活动记录流转 (TrackerService)**：在检测到用户切换上下文环境时，自动结束上一个活动记录，并无缝开启新的活动记录，自动写入本地数据库。
- [x] **硬件初级感知 (RawInputPlugin)**：利用 C++ 注入全局底层 Window Message，静默且低能耗地拦截全局键盘敲击总数、鼠标点击总数及滑动距离（px）。
- [x] **活动记录大盘接入**：界面时间轴（Timeline）真实叠加展示「实际」追踪记录色块，自动与日历日程并排呈现。`TrackerPage` UI 初始化完成，具备基础热力图。

---

## 三、待开发：精细化硬件遥测与洞察大盘 (Phase 2C - Telemetry V2)
*根据最新的UI规划和用户反馈，重新整编硬件遥测颗粒度与核心数据的呈现。*

### 3.1 C++ 硬件感知精细化升级 (RawInput C++ Refactor)
- [ ] **按键频次字典与输入序列 (Key Distribution & Sequence)**：摒弃仅统计“总按键数”的做法。基于虚拟键码 (`VKey`) 维护一个长度为 256 的数组，分别高频统计每一个按键（如 I, Space, Backspace, Ctrl 等）到底被敲击了多少次。同时增加系统设置入口，**默认关闭**记录按键顺序，开启后将允许进一步记录特定界面/应用下精确的字符输入序列（包含先后顺序）。
- [ ] **鼠标按键穿透解析**：拆解 `mouseClickCount`。依据 `RI_MOUSE_*_BUTTON_DOWN` 细分记录：
  - 左键 (Left)、中键 (Middle)、右键 (Right)。
  - 侧键：后退 (Backward/XButton1)、前进 (Forward/XButton2)。
- [ ] **滑轮滚动解析 (Scroll Detection)**：捕获 `RI_MOUSE_WHEEL` 及 `hWheel` 消息，统计并折算滚轮滑动像素距离 (px)。
- [ ] **移动距离矫正 (Cursor Calibrated Distance)**：暴露接口到 Dart 层，支持根据显示器 DPI 和用户设定，将原始像素位移转化为物理世界“米 (m)”的长度，支持“米”与“像素”无缝切换显示。

### 3.2 数据库扩展与活跃应用数据缝合 (Data Association)
- [ ] **数据库字段裂变**：升级 Drift 的 `app_database.dart` 至版本 4。在 `ActivityRecords` 表中追加追踪字段：`keyCount`, `mouseClicks`, `mouseMovePx`, `scrollPx`。
- [ ] **精准挂靠 (App-level Telemetry)**：`TrackerServiceNotifier` 在每一次“上下文环境切换”并结算 `ActivityRecords` 时，拉取当前时间段内的 **新增遥测增量 (Delta Input)** 并持久化写入该条活动记录。使最后的数据可以追溯到每个特定应用消耗了多少次按键和点击。

### 3.3 极致全能的今日洞察大盘构建 (Insight Dashboard UI)
依据新视觉图，彻底重构 `TrackerPage`，渲染：
- [ ] **A 级板块：顶格今日总览** (按键总数计算、鼠标点击总数)。
- [ ] **B 级板块：鼠标点击明细阵列** (左/中/右/后退/前进分块排列，配图表)。
- [ ] **C 级板块：距离与滚动** (鼠标移动物理距离展示、滚轮滑动距展示)。
- [ ] **D 级板块：按键分布天梯榜** (呈现单个按键如 “Space: 652” 的排行，并支持点击切入全局“历史按键键盘热力图与统计”页面)。
- [ ] **E 级板块：活跃应用榜单** (列出全天最耗时应用图标、名称，及其**绑定**的这应用下消耗的特定鼠标点击和按键数)。
- [ ] **F 级板块：历史记录趋势折线图** (利用 `fl_chart` 绘制支持 7天/30天 筛选维度的多维度趋势图表)。

---

## 四、待开发：阶段四（深层绑定与自然语义）
### 4.1 任务与深层效率融合
- [ ] **生产力画像指数**：根据「该排程任务所属应用」+「硬件级按键点击密度」，结合大模型算出每一项任务的真实效率专注指数。
- [ ] 对接本地 Ollama 或外部 DeepSeek 实现自然语言全自动排期（NLP）。
