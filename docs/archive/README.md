# FlowPlan Archive

This directory keeps historical source files that are no longer the active execution entry.

- `legacy-root-plans/`：旧根目录计划、任务、历史草案和早期 `CODEX.md`。它们只作为背景材料保留。

Active documents live in:

- `docs/FlowPlan_用户版完整开发计划_260426.md`
- `docs/planning/`
- `docs/architecture/`
- `docs/update.txt`
