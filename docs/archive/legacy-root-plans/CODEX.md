# CODEX 任务总表

本文件是当前项目的统一任务清单与产品共识记录。

- 从 2026-04-16 起，`CODEX.md` 作为后续开发的主任务面板。
- `implementation_plan.md` 与 `task.md` 保留为历史规划与阶段背景文档。
- 后续新增需求、优先级调整、阶段完成情况，都应同步更新到本文件。
- 若本文件与旧文档冲突，以本文件中的最新共识为准。

## 1. 当前产品目标

FlowPlan 当前的重点不再只是“采集到活动数据”，而是把“原始追踪日志、可读的工作会话、可查询的历史视图、逐步增强的分析展示”做成一套稳定闭环：

- 底层保留完整原始活动记录，不能因为前台窗口切换而丢失上下文。
- 上层提供适合人看的工作会话视图，避免碎片化、跳来跳去、难以理解。
- 展示层根据用户使用时长自适应，从短周期开始，再逐渐扩展到长期统计。
- 全部用户可见文案保持中文。

## 2. 已确认的产品共识

以下内容已经由体验反馈明确，应视为后续实现的硬约束。

### 2.1 中文优先

- 软件面向中文母语者。
- 所有用户可见文案、标签、说明、状态词、按钮文案都应为中文。
- 新增功能默认先写中文，不再混入英文占位文案。

### 2.2 “当前会话输入统计”语义必须重构

现有“当前会话输入统计”设计存在根本性问题：

- 它不应自动跟随当前前台应用实时切换。
- 用户进入 FlowPlan 自己的界面后，不应立刻把正在查看的外部应用数据切换成 FlowPlan。
- 用户想查看的，是刚才那段外部工作过程的持续记录，而不是“谁当前在前台就显示谁”。

新的目标语义：

- 该模块应改造成“可持续查看的工作会话统计”或“最近工作会话统计”。
- 当 FlowPlan 进入前台时，默认不抢占当前查看上下文。
- FlowPlan 应进入默认自排除列表，避免污染追踪结果。
- 用户需要能看到刚刚结束或刚刚活跃的外部工作会话，而不是被应用自身覆盖。

### 2.3 原始日志必须永久保留

追踪系统应具备类似日志系统的稳定记录能力：

- 所有活动信息都应持续输出到文件。
- 日志按天分割保存。
- 应用内需要提供历史查询能力，而不是只显示“此刻”的一小段临时状态。

建议方向：

- 以每日一个日志文件作为原始事实来源。
- 文件格式优先考虑 JSONL，便于追加写入、调试与恢复。
- 数据库可作为索引与查询缓存，但不应成为唯一真相来源。

### 2.4 “今日活动记录”不能过度碎片化

现有记录分割过于频繁，频繁切换窗口会严重破坏阅读体验。

新的目标语义：

- 原始层可以细粒度保留全部切换事实。
- 展示层必须提供“合并后的工作会话”视图。
- 短暂切换、无意义切换、回跳切换不能直接把页面切碎。
- 用户需要先看到连续的工作段，再按需下钻到原始切换明细。

这意味着后续需要显式区分两层数据：

- 原始活动日志层
- 派生工作会话层

### 2.5 热力图必须自适应时间尺度

固定展示一整年热力图，对新用户不友好。

新的展示原则：

- 刚开始使用时，先展示更短时间粒度。
- 随着数据积累，逐步扩展到更长期尺度。
- 用户也应能手动切换查看跨度。

目标时间跨度：

- 小时
- 日
- 月
- 年

建议展示策略：

- 使用时长很短时，默认小时视图。
- 积累到一定天数后，默认日视图。
- 积累到一定月份后，默认月视图。
- 长期使用后，再默认年视图。

## 3. 现有文档整理后的阶段结论

结合 `implementation_plan.md` 与 `task.md`，目前项目可归纳为以下状态。

### 3.1 已有基础能力

- 已完成基础 Flutter 应用框架。
- 已完成日历、任务、时间轴拖拽等基础界面能力。
- 已完成 iCal 导入、Outlook 同步、设置页等既有功能。
- 已具备追踪模块基础能力：
  - 前台窗口感知
  - 活动分类基础框架
  - 基础输入遥测采集
  - 活动记录写入
  - 追踪页基础展示

### 3.2 Phase 2C 原始目标

原规划中的 Phase 2C 重点是 Telemetry V2，包括：

- 更细的键盘分布与键序列采集
- 更细的鼠标按钮统计
- 滚轮检测
- 鼠标移动距离校准
- 数据库存储扩展
- 将遥测增量绑定到活动记录
- 追踪页分析面板升级

### 3.3 Phase 2C 现在需要调整方向

经过实际体验，Phase 2C 不应只追求“采集字段更丰富”，而应优先解决以下三个产品问题：

- 记录是否可信且可回看
- 会话是否可读且不碎
- 展示是否适合不同使用阶段

因此，Phase 2C 现在应视为“追踪系统从采集演示版走向可用版”的阶段。

## 4. 当前优先级重排

以下为新的优先级，从高到低排序。

### P0. 追踪语义修正

目标：修正当前会话视图的产品语义，避免 FlowPlan 自身抢占追踪结果。

待办：

- [ ] 明确“当前会话输入统计”的新命名与新定义
- [ ] 设计 FlowPlan 默认自排除策略
- [ ] 当 FlowPlan 进入前台时，冻结当前外部工作会话视图，而不是切换到自身
- [ ] 区分“当前外部工作会话”“最近工作会话”“原始日志片段”三个概念
- [ ] 统一追踪状态词，例如“采集中”“冻结查看中”“已结束”“已合并”

### P1. 原始日志系统

目标：把追踪数据升级为可追溯、可恢复、可查询的日志体系。

待办：

- [ ] 设计按天分割的日志文件结构
- [ ] 确定日志目录与命名规则
- [ ] 设计 JSONL 记录格式
- [ ] 明确日志写入时机、轮转规则、恢复策略
- [ ] 明确数据库与日志文件的职责边界
- [ ] 在应用内提供按日期查询历史记录的能力

建议日志记录类型：

- `sample`
- `session_open`
- `session_update`
- `session_close`
- `session_merge`
- `manual_annotation`
- `snapshot`

### P2. 工作会话合并层

目标：解决“今日活动记录”碎片过多的问题，把原始切换整理成可阅读的工作会话。

待办：

- [ ] 设计原始日志到工作会话的派生规则
- [ ] 为短暂切换建立缓冲或去抖策略
- [ ] 识别可并入同一工作段的相邻活动
- [ ] 为“临时查看 FlowPlan 界面”设计不打断会话的策略
- [ ] 增加“合并后工作会话视图”和“原始日志视图”的双层展示
- [ ] 支持后续人工合并、拆分、标注

优先考虑的合并原则：

- 短时间窗口切换不立即切断会话
- 同一任务上下文中的多窗口活动可合并
- FlowPlan 自身窗口默认不作为打断因素
- 原始事实永不丢失，展示结果可反复重算

### P3. 热力图与时间尺度浏览器

目标：把固定年热力图改造成自适应、多尺度的活动浏览器。

待办：

- [ ] 重新定义热力图组件的数据输入接口
- [ ] 支持小时、日、月、年四种尺度
- [ ] 支持根据数据积累时长自动选择默认尺度
- [ ] 增加手动切换控件
- [ ] 为新用户提供短周期展示，避免“大面积空白”
- [ ] 评估不同尺度下的颜色映射与密度表达

建议默认策略：

- 首日到数日：默认小时
- 数日到数周：默认日
- 数周到数月：默认月
- 长期使用：默认年

### P4. Telemetry V2 数据扩展

目标：在新的日志与会话架构上，继续补齐更细粒度输入遥测。

待办：

- [ ] 键盘分类统计
- [ ] 键序列摘要
- [ ] 鼠标左中右键分项统计
- [ ] 滚轮距离统计
- [ ] 鼠标移动距离校准
- [ ] 更细粒度输入摘要模型
- [ ] 隐私边界与脱敏策略复核

说明：

- Telemetry V2 不再单独作为展示目标，而是服务于日志、会话分析和统计展示。
- 键序列相关能力必须严格控制范围，默认应以摘要和计数为主，避免不必要的敏感内容保留。

### P5. 追踪页信息架构重做

目标：让追踪页从“临时状态面板”升级为“可回看、可分析、可切换层级”的工作台。

建议页面结构：

- [ ] 当前工作会话
- [ ] 今日工作会话
- [ ] 原始日志
- [ ] 活动热力图
- [ ] 输入统计与行为摘要
- [ ] 历史日期查询入口

重点要求：

- 页面默认首先服务“理解今天做了什么”
- 深入层再服务“查看某段原始切换细节”
- 不再让 FlowPlan 自身界面抢占主要信息位

### P6. 时间轴与任务绑定增强

目标：把追踪结果更自然地绑定到任务和日程，而不是停留在孤立记录。

待办：

- [ ] 把工作会话与任务候选建立更稳定的匹配关系
- [ ] 支持从会话片段回填任务
- [ ] 支持从任务查看关联活动证据
- [ ] 优化时间轴上合并段、拆分段、校正段的交互

### P7. LLM 校正与长期智能能力

目标：保留原规划中的智能纠偏与高级能力，但延后到基础可用性稳定之后。

待办：

- [ ] 周期性 LLM 纠偏
- [ ] 规则进化建议
- [ ] 生产力画像
- [ ] 自然语言排程增强

说明：

- 这些能力仍然重要，但目前不应优先于“日志可信”“会话可读”“展示可用”。

## 5. 建议的数据分层模型

为避免后续实现继续混淆，建议明确三层结构。

### 5.1 原始日志层

职责：

- 只追加，不覆盖
- 尽量保留完整事实
- 按天分割
- 允许重建上层结果

核心特征：

- 面向采集与审计
- 适合长期保留
- 可作为回放与故障排查依据

### 5.2 派生工作会话层

职责：

- 从原始日志重算出更稳定的工作段
- 做去抖、合并、归类
- 提供更适合用户阅读的结构

核心特征：

- 可重算
- 支持规则调整
- 可支持人工修正

### 5.3 聚合统计层

职责：

- 支撑热力图、趋势图、输入摘要、时间分布等展示
- 为不同时间尺度生成聚合桶

核心特征：

- 面向展示性能
- 可按小时、日、月、年聚合
- 不应替代原始日志

## 6. 当前推荐的命名与界面方向

以下名称为当前推荐方向，后续实现前可再微调，但语义应保持一致。

旧概念与新方向：

- “当前会话输入统计” -> “当前工作会话统计”或“最近工作会话统计”
- “今日活动记录” -> 拆为“今日工作会话”和“原始活动日志”
- “热力图” -> 升级为“活动热力图 / 时间尺度浏览”

建议追踪页一级入口：

- 当前工作会话
- 今日工作会话
- 原始活动日志
- 活动热力图
- 历史查询

## 7. 近期执行顺序建议

如果接下来继续推进开发，建议按以下顺序落地。

1. 先修正追踪语义与 FlowPlan 自排除逻辑
2. 再建立按天日志文件与历史查询基础
3. 再做工作会话合并层，解决碎片化
4. 再重做热力图时间尺度
5. 最后继续扩展 Telemetry V2 的细粒度字段与分析面板

原因：

- 如果没有稳定日志，后续很多展示都会缺乏可信基础。
- 如果没有会话合并层，追踪页再丰富也会继续显得混乱。
- 如果展示尺度不合理，新用户会持续感受到“有功能但不好用”。

## 8. 暂定完成判据

以下条件可作为这一轮追踪系统升级基本完成的判据。

- [ ] FlowPlan 自身不再轻易污染当前工作会话视图
- [ ] 追踪数据可按天稳定落盘
- [ ] 应用内可查询历史日志
- [ ] 今日记录不再因频繁切窗而严重碎片化
- [ ] 热力图支持小时、日、月、年切换
- [ ] 热力图默认尺度可根据使用时长自动调整
- [ ] 中文文案统一完成

## 9. 更新记录

### 2026-04-16

已将以下信息收敛进本文件：

- 根目录 `implementation_plan.md` 的总体架构与 Phase 2C 规划
- 根目录 `task.md` 的阶段任务与已完成基础能力
- 最近体验反馈中确认的产品修正方向：
  - 中文文案统一
  - 当前会话视图不再跟随 FlowPlan 前台切换
  - 原始日志按天永久保留
  - 今日活动记录需做去碎片化与工作会话合并
  - 热力图改为自适应时间尺度并支持手动切换

后续如果我们继续推进实现，应以本文件为准持续更新。

### 2026-04-16（开发进展补记）

本轮已落地第一批追踪系统改造，属于 P0 / P1 / P2 的首版实现：

- 已加入 FlowPlan 默认自排除逻辑
- 已把“当前工作会话”从前台窗口直接跟随，改成可冻结查看最近外部会话
- 已加入按天分割的原始日志文件写入
- 已在应用内接入原始日志浏览入口
- 已加入基于原始活动记录的工作会话合并视图
- 已重写追踪页主要中文文案与结构

本轮仍未完成的部分：

- 日志与数据库之间更完整的索引/恢复策略
- 工作会话更智能的跨应用合并规则
- 热力图的小时 / 日 / 月 / 年自适应时间尺度
- 历史日志的更强筛选与检索能力

### 2026-04-17

本轮继续推进了 P3 热力图重构，已完成首版时间尺度浏览器：

- 已把固定“近一年热力图”改造成通用热力图序列模型
- 已支持四种手动时间尺度：
  - 小时
  - 日
  - 月
  - 年
- 已加入基于使用时长的自动推荐尺度
- 已让热力图区随所选日期联动切换
- 已加入桶详情卡片，支持查看选中时间桶的活动时长与记录数

当前这一版的定位：

- 小时：查看某一天内 24 个小时的分布
- 日：查看某个月内每天的分布
- 月：查看某一年内每个月的分布
- 年：查看历史年度分布

后续仍可继续优化的点：

- 更细的交互反馈与视觉密度
- 年视图下更丰富的长期趋势表达
- 和历史日志、工作会话列表之间的联动筛选

### 2026-04-17（托盘与存储推进）

本轮已开始把“长期后台运行 + 用户可拿到数据库”落到实现层：
- 已在 Windows Runner 增加托盘常驻基础能力
- 已支持关闭主窗口时缩到托盘，而不是直接退出
- 已增加托盘菜单的“显示主窗口 / 退出”入口
- 已在设置页加入“关闭窗口时缩到托盘”与“开机自启动”开关
- 已把这两个新设置落到 SQLite 的 `app_settings` 表，而不是继续散落在额外本地配置里
- 已在追踪页加入“导出数据库副本 / 打开数据库目录”入口

本轮尚未完成、但已经明确进入下一步的事项：
- 将现有追踪原始日志从文件型过渡到数据库主存储
- 设计数据库导入能力，而不只是导出副本
- 让追踪页的原始记录查询完全基于数据库，而不是过渡性的文件读取
- 继续完善托盘常驻时的追踪状态反馈与退出前收尾
- 已将追踪原始日志主写入路径从 JSONL 文件切换为 SQLite 数据库表 `raw_activity_logs`
- 已补充数据库 schema 升级以承载原始追踪记录
- 已为旧版按天 JSONL 日志加入一次性迁移逻辑，尽量把既有历史导入数据库
- 已将追踪页的原始记录读取与存储位置展示改为数据库语义
- 已将 debug / profile / release 的本地存储目录拆分：release 保持 `flowplan`，debug 改为 `flowplan_debug`，profile 改为 `flowplan_profile`
- 已将非 release 版本的 SharedPreferences 前缀隔离，避免调试设置污染正式使用
- 已将 Windows 开机自启动注册表项在 debug 与 release 间拆分
- 已修复托盘图标回调解析错误，恢复左键唤起、双击唤起与右键菜单退出链路
- 已将“开机自启动”改为默认静默缩到托盘启动，避免登录 Windows 后直接弹出主窗口

### 2026-04-17（工作会话去碎片化推进）

本轮回到追踪主线，继续推进 P2 工作会话合并层：

- 已将工作会话分组从“仅同签名记录硬合并”升级为“双层规则”：
  - 同一上下文内的短间隔记录可直接合并
  - 中间夹着短暂、低输入的打断片段时，可桥接回前后同一上下文会话
- 已支持跨应用但同一工作上下文的短切换合并，降低频繁切窗造成的碎片感
- 已在追踪页工作会话卡片中展示：
  - 跨应用数量
  - 涉及应用预览
  - 已吸收的短切换次数

这使当前的“今日工作会话”更接近真实工作段，而不是机械地按每次前台切换切碎。

### 2026-04-17（历史查询与会话下钻推进）

本轮继续补强“可回看、可理解”的追踪页工作台：

- 已增加统一的历史筛选状态：
  - 搜索应用 / 分类 / 标题
  - 按应用筛选
  - 按分类筛选
  - 仅看有输入活动
- 已让筛选条件同时作用于：
  - 今日工作会话列表
  - 原始活动日志列表
- 已在页面中显示筛选后的命中数量，便于理解当前查看范围
- 已为工作会话增加展开能力，可查看：
  - 涉及的应用与分类
  - 被吸收的短切换说明
  - 组成该会话的原始记录明细与输入指标

这让追踪页开始具备“先筛选、再下钻、再核对原始证据”的基本闭环。

## 2026-04-17 主线开发更新

- 热力图已从只读展示推进为可联动查询入口。
- 小时视图支持把下方“工作会话”和“原始活动日志”缩小到具体小时。
- 历史筛选面板会显示当前热力图区间，并支持一键取消联动。
- 热力图已支持逐层下钻：
  - 年 -> 月
  - 月 -> 日
  - 日 -> 小时
- 切换日期和切换热力图尺度时，会清理旧的热力图区间联动，避免把上一层时间上下文错误带入当前查询。

## 2026-04-17 区间分析推进

- 热力图新增“区间分析”能力，支持直接查看选中年 / 月 / 日时间桶的聚合结果。
- 小时时间桶继续保留对下方工作会话和原始日志的列表联动筛选。
- 年 / 月 / 日时间桶现在同时支持两种动作：
  - 查看该区间的聚合分析
  - 继续下钻到更细的时间尺度
- 新增区间分析数据通路：
  - 按时间范围读取 ActivityRecord
  - 按时间范围读取原始追踪日志
  - 基于范围数据生成 ActivityInsights 和 WorkSession 聚合结果
- 追踪页新增“选中区间分析”面板，展示：
  - 区间总时长
  - 有效输入时长
  - 工作会话数
  - 原始日志数
  - 活跃应用数
  - 按键总数
  - 日志类型分布
  - 主要应用 / 主要分类
  - 代表性工作会话
  - 高输入片段

## 2026-04-17 跨日工作会话列表推进

- 区间分析面板已从“代表性会话摘要”升级为“区间工作会话列表”。
- 现在月 / 年 / 日时间桶都可以直接查看该区间内的跨日工作会话。
- 区间工作会话支持三种排序：
  - 最近优先
  - 时长优先
  - 输入优先
- 默认折叠显示前 12 段，避免长时间范围直接拉出超长列表；可一键显示全部或收起。
- 区间工作会话卡片已补齐完整细节：
  - 跨日时间范围
  - 涉及应用与分类
  - 原始记录数
  - 吸收的短切换次数
  - 键鼠输入指标
  - 展开查看原始记录与合并细节
- 区间分析面板新增日期分布摘要，帮助快速理解这段时间主要活跃在哪几天。

## 2026-04-17 区间分析二次筛选推进

- 区间分析面板已新增本地二次筛选：
  - 应用
  - 分类
  - 仅看有输入活动
- 筛选结果会同时联动以下内容：
  - 区间摘要卡片
  - 工作会话数量与列表
  - 原始日志数量与日志类型分布
  - 主要应用 / 主要分类
  - 高输入片段
- 区间工作会话列表现在会基于筛选后的 ActivityRecord 重新聚合，而不是只对原始会话结果做表层筛选。
- 当筛选后没有可聚合的活动记录时，面板会显示专门的空状态提示；如果仍有原始日志命中，也会明确提示。

## 2026-04-17 区间原始日志预览推进

- 区间分析面板新增“区间原始日志”段落，支持跨天查看所选时间桶内的原始追踪日志。
- 新增日志检索能力：
  - 关键词搜索标题、窗口、备注、类型
  - 按日志类型进一步筛选
  - 默认仅显示最新 30 条，可展开查看全部
- 即使当前筛选下没有可聚合的 ActivityRecord，只要仍有原始日志命中，也会继续展示日志明细，不再直接变成空白。
- 区间日志卡片已补齐可展开细节：
  - 跨天时间展示
  - 窗口标题 / 窗口类名 / 关联记录
  - 备注 / 全屏状态 / 按键序列
- 下一步建议：
  - 把区间日志结果按天分组折叠
  - 为日志检索补上导出当前结果
  - 继续推进“按天日志文件 / 历史查询页”主线

## 2026-04-17 按天日志文件与历史查询入口推进

- 追踪原始日志现在会在写入 SQLite 的同时，额外按天写入 `logs/YYYY-MM-DD.activity.jsonl` 文件。
- 启动时会尽量把数据库中的既有原始日志补写成按天归档文件，避免历史页只有新数据没有旧数据。
- 如果归档目录缺失，但数据库里仍有历史日志，历史页会尝试重新物化缺失的按天文件。
- 新增应用内历史查询入口：
  - 追踪页右上角菜单支持“查看历史日志文件”
  - 设置页新增“历史日志文件”入口
- 新增历史日志查询页能力：
  - 按天浏览归档文件
  - 查看日志目录与文件大小
  - 按关键词搜索标题 / 窗口 / 备注 / 应用 / 类型
  - 按日志类型筛选
  - 展开查看窗口类名、备注、按键序列等细节
- 下一步建议：
  - 为历史日志页补按时间段跳转与日期快速选择
  - 支持导出当前筛选结果
  - 把历史日志查询与后续“当前会话输入统计永久化”进一步汇合

## 2026-04-17 顺序键鼠记录与热力图推进
- 已新增顺序键鼠事件模型，支持记录每条键盘/鼠标事件的时间、顺序号、按键/按钮信息。
- 已在 Windows 原始输入桥接层补充前台窗口上下文采集，单条事件会尽量带上进程名、窗口标题和类名。
- 已新增 SQLite 表 `tracked_input_events`，用于持久化完整键鼠事件流，并保留 `record_id`、应用上下文与忽略标记。
- 已接入追踪采样流程：新的键鼠事件会随采样周期写入数据库，并与现有活动记录尽量建立关联。
- 已新增“导出完整键鼠记录”入口，导出格式为按时间顺序排列的 JSONL。
- 已新增独立“键鼠热力图”页面，支持按应用程序和时间范围筛选，并展示键盘/鼠标热点分布。
- 后续可继续推进：
  - 让热力图页支持更细粒度统计摘要与筛选导出
  - 基于 `tracked_input_events` 做更完整的输入行为分析和跨会话回放
  - 优化活动记录与输入事件在快速切窗场景下的自动归属精度

## 2026-04-17 输入行为分析面板推进
- 已基于 `tracked_input_events` 补齐输入行为分析摘要面板，进入页面后可直接看到：
  - 最高频按键
  - 最活跃应用
  - 峰值时段
  - 输入密度
- 已在热力图页补充“高频按键”榜单，展示按键次数与键盘输入占比。
- 已在热力图页补充“应用内输入强度”榜单，基于键盘、点击、滚轮、鼠标移动和活跃分钟生成强度对比。
- 已在热力图页补充“时间段分布”面板，按 24 小时展示输入强度柱状分布，并列出峰值时段。
- 已把“活跃分钟”“平均每活跃分钟事件数”“应用事件占比”等派生指标接入汇总模型，供后续更深入分析复用。
- 下一步建议：
  - 继续做跨会话输入回放与日志钻取
  - 为输入分析增加更多组合筛选与导出
  - 评估是否需要把键盘/鼠标分析入口进一步挂到追踪主页摘要中

## 2026-04-18 键鼠热力图刷新与顺序输入日志推进
- 已将“键鼠热力图”页面从被动跟随追踪刷新，调整为页面内手动刷新模式：
  - 后台追踪继续写入数据
  - 当前页面不再随采样自动整页刷新
  - 保留右上角手动刷新按钮
  - 用户切换筛选条件时仅刷新当前查询结果
- 已在热力图页补充刷新状态提示与最后刷新时间，减少查看过程中被打断的感觉。
- 已把 `tracked_input_events` 接入按天 JSONL 归档文件：
  - 新文件格式为 `logs/YYYY-MM-DD.input-events.jsonl`
  - 每条记录保留顺序号、时间、事件类型、按键码、按键标签、token、应用上下文等完整信息
  - 归档顺序与数据库查询顺序保持一致，便于后续回放与检索
- 已增加输入事件归档的历史补写逻辑：如果数据库中已有输入事件，但按天归档文件缺失，会在初始化时尽量补齐。
- 下一步建议：
  - 为输入事件日志补应用内查询页或“打开日志目录”入口
  - 在追踪主页增加“最近输入序列”或“最近输入事件”只读预览
  - 继续推进输入事件与工作会话、历史日志之间的下钻联动

## 2026-04-18 输入历史页与启动链路推进
- 已新增“完整输入历史”页面，支持：
  - 按天浏览 `YYYY-MM-DD.input-events.jsonl`
  - 查看单日完整输入事件顺序
  - 按关键词搜索按键、字符、应用、窗口、分类、序号
  - 按输入类型筛选：按键 / 鼠标按钮 / 滚轮 / 鼠标移动
  - 选择是否包含 FlowPlan 自排除记录
- 已在追踪页新增“最近输入预览”卡片：
  - 默认显示最近 12 条外部应用输入摘要
  - 保持与完整输入历史页的入口联动
  - 不再让主页只能依赖热力图页或导出文件查看输入事件
- 已在追踪页右上角菜单新增“查看完整输入历史”入口。
- 已修复设置页“开机自启动”开关无法关闭的问题：
  - 不再把数据库里的旧值和实际系统状态做 `actual || stored` 式合并
  - 现在以系统真实状态为准回写到数据库
  - 切换后再以系统实际应用结果回写页面状态
- 已将 Windows 开机自启动从注册表 `Run` 项改为“计划任务”方案：
  - 登录后以最高权限启动
  - 默认带 `--startup-to-tray` 静默缩到托盘
  - debug / release 继续保持独立任务名，避免互相覆盖
  - 如果发现旧版注册表自启动项，会尽量迁移到计划任务
- 已将 Windows Runner manifest 改为 `requireAdministrator`，应用现在要求管理员权限启动。

这一轮之后，当前主线建议继续聚焦：
- 基于 `tracked_input_events` 做更完整的输入行为分析面板与跨视图联动
- 继续推进“工作会话去碎片化”规则，而不是只增加展示组件
- 评估输入历史页是否需要补“按会话回放”与“导出当前筛选结果”

## 2026-04-18 主页输入分析与会话去碎片化推进
- 已在追踪主页新增“输入行为分析”面板，直接展示选中日期的：
  - 高频按键
  - 主力应用
  - 应用内输入强度
  - 峰值时段
  - 小时级时间段分布
- 主页输入分析面板直接复用 `tracked_input_events` 聚合结果，不再只存在于独立热力图页里。
- 已新增按日期的输入行为汇总 Provider，后续可继续被更多页面复用。
- 已继续增强工作会话去碎片化规则：
  - 不再只支持“单个短打断”桥接
  - 现在可桥接一串连续短打断，再回到同一上下文的情况
  - 现在可桥接“中间只是短暂切到 FlowPlan 等自排除窗口”的情况，避免前后同一工作上下文被硬切断
- 已将会话卡片中的文案从“短切换”统一调整为更准确的“打断”，以覆盖短切窗和自排除窗口两种场景。

下一步可继续推进：
- 为输入行为分析增加更多“组合键 / 连续输入片段 / 应用切换前后强度变化”视角
- 继续观察新的去碎片化规则在真实使用中的边界，再决定是否补“人工合并 / 拆分”入口

## 2026-04-18 输入行为分析与工作会话双向联动
- 已补全“输入行为分析 ↔ 工作会话列表”的双向联动：
  - 现有链路继续保留：点击输入行为分析中的应用强度 / 时段分布，可直接筛选当天工作会话与原始日志
  - 新增反向链路：点击工作会话卡片，可反向联动上方输入行为分析
  - 在会话展开后的原始记录行中，也可以继续做更细粒度的应用 / 时段联动
- 已补充当前联动状态可见性：
  - 会话卡片会展示“已联动应用分析 / 已联动时段分析”状态
  - 工作会话区标题文案已明确提示可从会话卡片反向联动到输入行为分析
- 当前这部分已经把主线里的这一项收口，后续输入分析继续深化时，可以直接基于同一套联动状态扩展。

## 2026-04-18 任务绑定闭环与任务级追踪证据
- 已将“任务绑定”从追踪页单点操作推进为闭环能力：
  - 任务详情页新增“追踪证据”区
  - 可直接查看当前任务关联的工作会话、原始活动记录、输入行为证据
  - 可从任务详情页直接取消某条原始记录的任务关联
- 已把 `tracked_input_events` 的任务级聚合补齐：
  - 支持按任务汇总输入事件强度
  - 支持按任务查看高频按键、峰值时段、主力应用
  - 支持按任务查看最近输入事件
- 已把追踪页历史筛选进一步工作台化：
  - 新增“按任务筛选”
  - 可结合应用 / 分类 / 是否有输入 / 热力图区间一起筛选
  - 原始日志列表会跟随任务筛选收窄到对应活动记录
- 这部分对应主线推进：
  - P3 追踪页工作台化：筛选维度更完整
  - P4 Telemetry V2 深化：任务级输入行为分析落地
  - P5 任务绑定增强：任务页可直接回看真实执行证据

## 2026-04-18 追踪主页瘦身与详细数据分流
- 已针对“追踪”主页内容过多、渲染压力过大的问题完成第一轮结构拆分：
  - 主页继续保留热力图、区间分析、当前状态、日报摘要、输入行为分析
  - 工作会话列表、原始日志预览、重型筛选迁移到新的“追踪详细数据”二级页面
- 已在主页新增“详细数据入口”卡片：
  - 集中承接“查看今日详细数据 / 查看完整输入历史 / 查看历史日志文件”
  - 明确告知主页只负责摘要与分析，二级页负责细查与回溯
- 已新增“追踪详细数据”页并接入路由：
  - 复用既有历史筛选状态
  - 承载当日工作会话列表
  - 承载原始日志预览
  - 保留任务绑定、打开任务、按应用/时段回链到输入行为分析的能力
- 已调整“输入行为分析 ↔ 工作会话列表”的结构适配方式：
  - 在详细页点击工作会话的应用/时段分析按钮后，会回到主页并保留联动条件
  - 这样既保住双向联动，也避免把分析面板和明细面板继续塞回同一页
- 已对原始日志做二级页限量预览：
  - 本页只渲染部分日志，避免二级页再次无限膨胀
  - 完整历史仍由独立日志页承接

后续仍建议继续推进：
- 继续压缩详细页首屏负担，必要时为日志和会话加入分页、分组或懒加载
- 重新梳理“当前会话 / 最近会话 / 明细工作台”的命名与跳转路径
- 继续观察主页瘦身后的真实卡顿改善情况，再决定是否做更激进的拆分
## 2026-04-18 追踪性能热点收口
- 已开始针对追踪卡顿的底层热点做专项收口：
  - Windows 原生键鼠监听不再为每一个输入事件重复抓取前台窗口进程信息
  - 前台窗口上下文改为在原生后台线程内定时刷新并缓存，输入事件只读取缓存结果
  - Dart 侧 `RawInputEvent` 不再在解码阶段立即做逐事件本地时间转换，改为按需惰性转换
  - 输入行为汇总阶段移除了逐条 `toLocal()` 与分钟级 `DateTime` 重建，降低手动刷新时的主线程负担
  - 追踪主页的手动刷新链路改为并行加载热力图、日报、输入分析和区间分析，缩短单次刷新阻塞时间
- 后续仍需继续观察真实构建版本下的体感：
  - 若仍有明显卡顿，再继续下沉重型统计到二级页面或拆成分页 / 懒加载
  - 若原生输入事件量仍过大，再评估是否为鼠标移动分析增加更轻量的聚合读取通道
## 2026-04-19 安卓适配主线（新）

### 已确认的产品边界

- 仅支持 Android 16 及以上。
- 当前阶段只做安卓，不做 iOS / iPadOS。
- 必须同时兼容手机端和平板端。
- 安卓端第一阶段只接入应用使用记录，不接入本地键盘鼠标输入记录。
- 未来允许手机查看电脑端同步日志，但不纳入当前阶段。

### 已确认的技术约束

- 安卓端应用使用记录采用系统 `PACKAGE_USAGE_STATS`。
- 第一阶段不做后台常驻，不做前台服务，不解决应用在后台长期存活问题。
- 读取策略改为“打开应用时增量读取并处理”：
  - 启动 FlowPlan
  - 检查权限
  - 读取上次游标之后的 Usage Events
  - 还原前台应用片段
  - 写入现有追踪数据层
  - 推进游标

### 当前代码结构结论

- `activity_records` 已经存在 `packageName` 字段，可直接复用，不需要另建安卓专用主表。
- `raw_activity_logs` 与 `activity_records` 已经是当前追踪主链路，安卓端应沿用。
- 当前 `TrackerServiceNotifier` 强绑定 Windows：
  - `Platform.isWindows`
  - `WindowSensor`
  - `RawInputService`
- 安卓工程目前仍是默认 Flutter 外壳，`MainActivity` 尚未接入任何原生追踪桥接。
- 当前展示层大量依赖 `processName` / `windowTitle`，安卓导入时必须同时补齐“稳定标识”和“可读名称”。

### 安卓端统一数据要求

为后续跨端同步与多设备区分，安卓主线要求引入统一来源元数据：

- `deviceId`
- `platform`
- `source`
- `packageName`
- `componentName`
- `appLabel`

说明：

- `deviceId` 必须尽早落地，后续 Windows 端也要补齐。
- `platform` 至少区分 `windows` 与 `android`。
- `source` 继续保留来源语义，例如 `windows_auto`、`android_usage_stats`。

### 安卓端记录映射原则

安卓端导入后的记录先遵循以下兼容策略：

- `packageName`：真实包名，例如 `com.tencent.mm`
- `processName`：第一阶段保留为稳定筛选标识
- `windowTitle`：第一阶段优先写应用显示名 `appLabel`
- `className`：如可获得则写 Activity / Component 名
- `source`：写 `android_usage_stats`
- `keyCount` / `mouseClicks` / `mouseMovePx` / `scrollPx`：全部为 `0`
- `keySequence`：固定为空

### 安卓适配实施阶段

#### A0. 安卓环境收口

- [ ] 将 `android/app/build.gradle.kts` 的 `minSdk` 提升到 36
- [ ] 保持 `compileSdk = 36`、`targetSdk = 36`
- [ ] 检查 Manifest、主题、窗口配置对手机 / 平板的适配
- [ ] 明确安卓端继续沿用 debug / profile / release 存储隔离

#### A1. 平台采集层抽象

- [ ] 将当前追踪上下文模型从 Windows 前台窗口快照抽象为平台通用上下文
- [ ] 将 `TrackerServiceNotifier` 对 `WindowSensor` 的直接依赖改为平台采集接口
- [ ] 保留 Windows 现有实现作为一个平台分支
- [ ] 新增 Android Usage Stats 导入实现分支

#### A2. 安卓原生桥接层

计划采用 MethodChannel，首批桥接方法：

- [ ] `getUsageAccessPermissionStatus`
- [ ] `openUsageAccessSettings`
- [ ] `queryUsageEvents(sinceMillis, untilMillis)`
- [ ] `resolveAppLabels`（可选）

原生侧返回的首批字段至少包含：

- `timestampMillis`
- `packageName`
- `className`
- `eventType`
- `appLabel`

#### A3. 增量导入与游标推进

- [ ] 新增安卓导入游标设置，例如 `tracker.android_usage_stats_cursor_millis`
- [ ] 每次启动时仅导入游标之后的增量 Usage Events
- [ ] 将 Usage Events 还原为前台应用会话片段
- [ ] 将片段写入 `activity_records`
- [ ] 同时写入 `raw_activity_logs`
- [ ] 导入成功后推进游标
- [ ] 导入失败时保留旧游标，避免跳过数据

#### A4. 安卓端会话切分与去噪

- [ ] 忽略系统 UI、桌面启动器、最近任务页等噪声包
- [ ] 对特别短暂的前台切换做去噪
- [ ] 同一应用短时间回切时自动合并
- [ ] 暂不追求秒级实时，优先保证补算结果可信

#### A5. 安卓端页面裁剪

- [ ] 安卓端隐藏“键鼠热力图”
- [ ] 安卓端隐藏“输入行为分析”
- [ ] 安卓端隐藏“导出完整键鼠记录”
- [ ] 安卓端隐藏“按键序列记录”
- [ ] 安卓端保留：
  - 今日总结
  - 热力图
  - 应用排行
  - 工作会话
  - 原始日志查询入口
  - 数据库导出

#### A6. 手机 / 平板双形态适配

- [ ] 手机端采用单列 summary-first 布局
- [ ] 平板端采用双列或主次栏布局
- [ ] 详细信息不直接堆到一级页面
- [ ] 所有安卓可见文案保持中文

### 安卓主线执行顺序

1. 先做 A0：收口 Android 16+ 边界
2. 再做 A1：平台采集抽象
3. 再做 A2：MethodChannel + Usage Stats 原生桥接
4. 再做 A3：增量导入、游标推进、落库
5. 再做 A4：去噪与会话合并
6. 最后做 A5 / A6：移动端页面裁剪与手机 / 平板适配

### 当前阶段明确不做

- [ ] iOS / iPadOS
- [ ] Android 16 以下兼容
- [ ] 安卓后台常驻追踪
- [ ] 安卓本地键盘鼠标记录
- [ ] 安卓触摸热力图
- [ ] 手机查看电脑同步日志
- [ ] 云同步与跨端冲突合并
## 2026-04-19 安卓适配进度补记

### 本轮已完成

- 已将 Android 最低版本收口到 Android 16（`minSdk = 36`），并保留 `compileSdk = 36`、`targetSdk = 36`
- 已在 `AndroidManifest.xml` 中加入 `PACKAGE_USAGE_STATS`
- 已在 `MainActivity.kt` 中接入 Usage Stats MethodChannel
- 已落地首批桥接方法：
  - `getUsageAccessPermissionStatus`
  - `openUsageAccessSettings`
  - `queryUsageEvents(sinceMillis, untilMillis)`
- 已新增安卓端增量导入服务：
  - 游标键：`tracker.android_usage_stats_cursor_millis`
  - 启动 / 手动刷新时按游标读取系统使用记录
  - 将 Usage Events 还原为前台应用会话片段
  - 写入 `activity_records`
  - 同时写入 `raw_activity_logs`
- 已接入设备元数据基础能力：
  - `deviceId`
  - `platform`
  - `source`
  - `packageName`
  - `appLabel`
- 已完成第一轮安卓去噪：
  - 忽略本应用、系统界面、常见桌面启动器
  - 忽略过短会话
  - 对短时间同应用回切做自动合并
- 已完成追踪主页第一轮安卓裁剪：
  - 隐藏按键序列开关
  - 隐藏键鼠热力图入口
  - 隐藏输入行为分析面板
  - 隐藏完整键鼠历史入口
  - 在“当前工作会话”中改为展示安卓端说明文案
  - 当未授权 Usage Access 时显示授权提示卡，并可直接跳转系统授权页

### 当前状态

- 平台采集差异已抽象为统一平台来源模型，`TrackerServiceNotifier` 仍保留具体采样状态机，但平台能力判断已收口
- `activity_records` 侧已通过数据库迁移持久化 `device_id` 与 `platform`
- 安卓手机 / 平板已具备基础响应式布局约束，仍需真机继续校准视觉细节
- 安卓端 Usage Access 权限、设备标识和同步入口已接入设置页
- 安卓端导入后的更细分类规则仍可继续在后续追踪智能融合中深化

### 下一步建议顺序

1. 在真机手机和平板上验证 P5 布局、权限、提醒和同步入口
2. 继续补齐隐私与权限说明，明确 Usage Stats、精准闹钟、通知、Outlook OAuth 的用途
3. 继续推进 P6：Outlook 双向同步深化和跨端数据一致性整理

## 2026-04-21 日历本 / 任务本规则补全

- 已修复左侧主导航“时间轴 / 本周 / 月视图 / 追踪 / 设置”点击后内容切换但高亮不更新的问题：
  - `CalendarShell` 不再通过 `child` 类型猜测当前页面
  - 改为直接使用当前路由状态判断选中项
- 已补上任务与日程详情页的删除能力：
  - 任务详情页支持直接删除任务
  - 本地日程详情页支持直接删除日程
  - Outlook 只读同步日程继续禁止在 FlowPlan 内修改或删除
- 已移除左侧 FlowPlan 下方“日程属于日程本，任务属于任务本”的说明文案
- 已继续推进“本包含条目”的边界一致性：
  - 任务本现在支持“归档”与“直接删除”两种路径
  - 已归档任务本新增“恢复”与“彻底删除”入口
  - 归档或删除任务本时，原有任务会自动迁移到其他可用任务本，避免任务脱离容器单独存在
  - 删除本地日历本时，原有本地日程会自动迁移到其他可用本地日历本
- 已收紧 iCalendar 导入导出规则：
  - 导入导出现在明确以“本地日历本”为操作对象
  - Outlook 只读同步日历不会被写回，也不会混入导出目标
  - 导出文件名已增加 Windows 非法字符清理，避免保存失败

### 后续建议

- 继续补“任务本 / 日历本”在更多边界下的一致性：
  - 默认本、只读本、归档本的更多提示与限制
  - 导入冲突、重复事件、跨本迁移的提示策略
  - 任务本 / 日历本导入导出时的范围选择与覆盖策略

### 2026-04-22 完整数据库备份 / 恢复闭环

- 已将“导入 / 导出”页面扩展为“导入 / 导出与备份”入口的一部分：
  - 支持导出完整数据库副本
  - 支持打开当前数据库目录 / 查看数据库位置
  - 支持在页面内选择数据库备份并暂存为“待恢复副本”
- 已补齐完整数据库恢复闭环：
  - 应用启动前会先检查是否存在待恢复副本
  - 若存在，则先应用恢复，再打开主数据库
  - 恢复前会保留一份 `flowplan.before_restore.db` 作为回退副本
- 已补齐恢复状态可见性：
  - 导入导出页会显示“已暂存待恢复副本”
  - 展示来源文件与准备时间
  - 支持在真正重启前取消这次待恢复操作
- 当前这部分对应的主线来源：
  - `plan260422.txt` 中关于容器导入导出、数据库可导出可恢复、用户可直接拿到数据库的要求

### 2026-04-22 iCalendar 导入防重

- 已补齐 `.ics` 导入的本地防重策略：
  - 同一日历本内，若导入项 UID 已存在，则改为覆盖更新现有日程
  - 若导入项没有可用于匹配的稳定 UID，但标题与起止时间和现有本地日程完全一致，则自动跳过
- 导入结果文案现在会明确反馈：
  - 新建多少条
  - 更新多少条
  - 跳过多少条重复日程

### 2026-04-22 Outlook 镜像索引手动清理

- 已将 Outlook 设置页中的“镜像索引健康度”从纯提示面板继续推进为可执行维护动作：
  - 当满足双向同步与读写授权条件时，可直接执行“清理失效镜像”
  - 不必为了清理失效任务镜像而强制跑一整轮日历同步
- 已补齐清理逻辑：
  - 本地任务已不存在
  - 任务已脱离任务本
  - 任务本已解绑 Outlook 镜像容器
  - 镜像目标容器已变更
- 清理成功后会：
  - 删除 Outlook 侧对应的 FlowPlan 托管镜像事件
  - 同步移除本地镜像索引
  - 刷新设置页中的镜像健康度状态

### 2026-04-22 日历本 / 任务本默认容器一致性

- 已补齐默认容器的一致性规则：
  - 本地日历本始终保持一个可用默认项
  - 未归档任务本始终保持一个可用默认项
  - 删除或归档默认容器后，会自动把其他可用容器补位为新的默认项
  - 若当前没有其他可用容器，系统会自动创建本地默认日历本或“收件箱”任务本承接
- 已补齐页面层可见性：
  - 日历本管理页与任务本管理页现在会显示“默认”标记
  - 本地日历本与未归档任务本新增“设为默认”操作入口
  - 删除 / 归档确认文案会明确提示默认容器自动补位行为

### 2026-04-23 iCalendar 导入导出策略补全

- 已继续补齐日历本导入导出的边界策略：
  - `.ics` 导入新增三种模式：智能合并、仅追加、清空后导入
  - 智能合并会继续沿用 UID 更新与标题 / 时间防重规则
  - 仅追加不会改写已有日程，遇到重复项只跳过
  - 清空后导入会先弹出确认，并在仓储层以原子替换方式重建当前日历本内容
- 已补齐导出范围选择：
  - 支持只导出当前本地日历本
  - 支持把全部本地日历本合并导出到一个 `.ics` 文件
  - 页面规则说明会提示 `.ics` 合并导出不保留日历本边界，若要保留完整容器关系应使用完整数据库备份
- 已将导出逻辑从固定大时间范围监听改为仓储层按日历本直接查询，避免为了导出拉取无关范围数据

### 2026-04-23 容器默认规则继续接入条目创建

- 已将日历本默认规则继续接入 `.ics` 导入链路：
  - 导入到某个本地日历本时，导入日程会继承该日历本颜色
  - 导入日程会继承该日历本的“默认阻挡自动排程”设置
  - 这使“课表”等默认阻挡排程的日历本在批量导入后能直接参与排程避让
- 已补齐详情页选择器的默认容器可见性：
  - 新建 / 编辑日程时，日历本选择器会显示默认日历本标记
  - 新建 / 编辑任务时，任务本选择器会显示默认任务本标记

### 2026-04-23 P2 提醒系统运行时闭环

- 已依据 `plan260422.txt` 的 P2 主线继续推进提醒能力：
  - 新增运行时提醒服务，应用启动后按分钟扫描未来 24 小时内的日程与任务
  - 日程提前提醒读取设置中的默认提前提醒分钟数
  - 任务支持计划开始提醒、截止提醒与截止风险提醒
  - Windows 端通过原生桌面壳新增置顶强提醒兜底
  - Android 端补充通知权限声明，并接入本地通知基础触达
- 已在设置页补充“运行时提醒服务”说明
- 已将 Outlook 同步入口文案更新为“只读 / 双向同步 / 暂停同步”的三态语义
- 已将版本链路同步提升到 `v1.1.3`

### 当前提醒系统仍未完成

- 还没有完成 Windows 标准右下角系统通知，当前是置顶弹窗兜底
- 还没有完成 Android 精准闹钟权限检测与调度
- 还没有完成应用关闭、设备重启、时区变化、同步更新后的提醒重建机制
- 还没有完成偏离计划提醒与自动顺延联动

### 2026-04-23 P0 Outlook 安全边界完整收口

- 已依据 `plan260422.txt` 的 P0 主线完成 Outlook 同步模型与安全边界收口：
  - 普通 Outlook 日历继续保持只读，不允许 FlowPlan 写回、覆盖或删除
  - 双向同步只允许写入 FlowPlan 托管的 Outlook 专属日历容器
  - 任务镜像只写入明确绑定的 `FlowPlan 任务本 - ...` 专属容器
- 已升级任务镜像正文：
  - 增加用户可读说明，明确建议回 FlowPlan 编辑
  - 补齐任务字段、提醒字段、锁定排程、自动排程、拆分规则等内容
  - 增加 `FLOWPLAN_TASK_META_START / END` 机器可读恢复元数据
- 已新增任务镜像字段快照与稳定指纹：
  - 镜像索引会记录上次写回时的字段快照
  - 设置页可识别哪些任务自上次镜像后有字段变更
- 已收紧冲突策略：
  - 任务镜像写回失败时不再静默删除并重建远端事件
  - 改为记录为冲突候选，避免远端被用户删除或修改后被无感覆盖
- 已补齐设置页诊断能力：
  - 新增“诊断与冲突”面板
  - 展示字段级冲突候选
  - 支持导出 Outlook 同步 Markdown 诊断报告
- 已将版本链路同步提升到 `v1.1.4`

### P0 后续边界

- P0 已完成安全模型、可诊断性和基础冲突候选界面
- 更复杂的远端字段级合并、跨设备冲突合并、批量维护和远端删除双向语义继续放入 P6 深化

### 2026-04-23 P1 结构化容器归档闭环

- 已依据 `plan260422.txt` 的 P1 主线完成日历本 / 任务本结构化迁移能力：
  - 新增 FlowPlan 专属 `.flowplan.json` 容器归档格式
  - 归档内容保留日历本、任务本、容器默认规则、日程、任务和项目归属关系
  - 用户可以在导入 / 导出页选择要导出的本地日历本和任务本
- 已补齐结构化导入策略：
  - 智能合并：同名容器合并，同 UID 项目更新，其他项目新增
  - 仅追加：已有 UID 跳过，只添加新项目
  - 替换同名容器内容：只清空同名容器内部项目，不影响其他容器
- 已补齐导入前确认与回滚保障：
  - 导入前展示容器级差异预览
  - 明确新建、合并、新增、更新、跳过、移除数量
  - 真正写入前自动生成数据库回滚备份
- 已补齐任务仓储能力：
  - 新增按任务本清空任务的方法，供结构化替换导入使用
- 已将 `plan260422.txt` 中 P1 剩余结构化导入导出项目打勾
- 已将版本链路同步提升到 `v1.1.5`

### P1 后续边界

- P1 已形成“容器归属完整、可选择性迁移、可预览、可回滚”的主线闭环
- 更细粒度的逐条字段合并、导入后可视化回滚按钮、跨设备增量冲突合并可继续放入 P6 或后续数据治理专项

### 2026-04-23 P2 提醒系统完整收口

- 已依据 `plan260422.txt` 的 P2 主线完成提醒系统闭环：
  - 运行时提醒服务继续按分钟扫描日程、任务和截止风险
  - 新增计划偏离提醒：自动排期任务已到计划时间，但没有绑定追踪证据时触发提醒
  - 应用启动后会重建未来系统提醒队列
  - 默认提醒时间变更后会立即重建系统提醒队列
  - 运行时每 15 分钟会刷新一次未来系统提醒队列
- 已补齐 Android 系统级提醒：
  - 新增原生 `AlarmManager.setExactAndAllowWhileIdle` 精准闹钟调度
  - 未来 7 天内的日程提醒、任务开始提醒、任务截止提醒、任务风险提醒会写入系统队列
  - 新增 `ReminderAlarmReceiver` 负责应用关闭后的通知触达
  - 新增 `ReminderBootReceiver` 负责设备重启、时间变化、时区变化、应用更新后的提醒恢复
  - 设置页新增精准闹钟权限状态、已写入提醒数量、上次重建时间、跳转权限设置和手动重建入口
- 已补齐 Windows 提醒触达：
  - 原生提醒保留置顶强提醒弹窗
  - 同时新增系统托盘气泡通知，更接近右下角系统通知体验
- 已将 `plan260422.txt` 中 P2 剩余项目打勾
- 已将版本链路同步提升到 `v1.1.6`

### P2 后续边界

- P2 已形成跨平台提醒主线闭环
- 后续更深层能力继续放入 P4 / P3：偏离后询问是否自动顺延、确认后局部重排、多组工作时间参与提醒策略、追踪证据自动绑定优化

### 2026-04-23 P3 智能排期核心升级

- 已依据 `plan260422.txt` 的 P3 主线完成智能排期核心升级：
  - 设置页新增“多组工作时间”，支持周一到周日分别配置多个工作时段
  - 默认工作时间改为更贴近日常的上午、下午、晚间多段结构，支持午休和晚饭休息
  - 排期器从固定 8-22 点单段窗口，升级为读取多组工作时段并在工作窗口内寻找空档
  - 排期器读取“阻挡自动排程”日程作为固定阻挡块，并继续避让已排任务
  - 任务详情页新增“锁定排程”，锁定任务不会被一键重排移动
  - 一键重排现在从当前时间之后做局部重排，只移动允许自动排期且未锁定的任务
  - 不可拆分任务必须找到完整连续空档才会写入
  - 可拆分任务在没有连续空档时生成多段拆分建议，避免当前单开始时间模型错误占用休息时段
  - 一键重排后可查看“排程变更记录”，用中文解释每个任务的安排、重排、未排入或拆分建议原因
- 已将 `plan260422.txt` 中 P3 项目更新为完成 / 保守完成状态
- 已将版本链路同步提升到 `v1.1.7`

### P3 后续边界

- P3 已形成多组工作时间、局部重排、锁定避让和排期解释日志的核心闭环
- 真正的“一个任务多段持久化显示”已在 `v1.1.8` 通过任务排程片段模型与时间轴片段渲染补齐

### 2026-04-23 P3 排程片段与人为确认边界

- 已继续完善 P3 中此前保守处理的可拆分任务能力：
  - 新增 `task_schedule_segments` 表，保存任务多段排程片段
  - 新增任务排程片段仓储，支持按任务替换片段、按日期读取片段
  - 一键重排现在只生成预案，用户必须在确认弹窗中点击“确认应用”后才会写入任务时间和排程片段
  - 可拆分任务确认后会写入多段排程片段，时间轴按片段渲染
  - 时间轴如果发现任务已有片段，会隐藏原本的整块任务，避免一个大块跨越午休 / 晚饭等休息时段
  - 时间轴拖拽任务、拖拽日程、调整任务时长、调整日程时长均新增确认弹窗
- 已新增 `data_operation_logs` 初版数据操作日志表：
  - 一键重排应用会记录日志
  - 排程片段替换会记录日志
  - 时间轴手动修改任务 / 日程会记录日志
- 已将“未来全量数据操作审计”补充到 `plan260422.txt`：
  - 所有任务、日程、容器、同步、导入导出等数据操作后续都应可查
  - 高频“记录追踪数据”本身明确不纳入该审计范围，避免日志爆炸
- 已将版本链路同步提升到 `v1.1.8`

### P3 当前边界

- P3 已完成多组工作时间、局部重排、锁定避让、可拆分任务多段持久化、时间轴片段渲染、人工确认和初版审计日志
- 数据操作日志目前只覆盖 P3 相关高风险变更；全软件所有数据操作统一审计仍是后续专项

### 2026-04-23 P4 追踪与排期双向联动

- 已依据 `plan260422.txt` 的 P4 主线完成追踪与排期执行反馈闭环：
  - 新增计划执行反馈服务，建立稳定的“当前计划项”和“当前追踪项”模型
  - 当前计划项优先读取 `task_schedule_segments`，没有片段时回退到任务 `dtstart`
  - 当前追踪项读取当前活动记录，若没有当前活动则读取最近活动记录
  - 若追踪记录已绑定当前任务，或活动文本与任务关键词匹配，则视为正常执行，不打扰用户
  - 若任务已开始超过宽限时间，且追踪活动未绑定任务、未匹配任务关键词，或属于游戏 / 娱乐 / 社交等明显偏离分类，则生成偏离候选
  - `CalendarShell` 已接入偏离监听，检测到偏离后弹出应用内确认提示
  - 用户选择“生成重排预案”后，排期器会把当前偏离任务和后续可自动排期任务从当前时间之后重新排
  - 顺延重排仍沿用 P3 的预案确认机制，必须二次确认后才会写入任务时间和排程片段
  - 偏离提示的接受 / 暂不处理，以及确认后的顺延重排都会写入 `data_operation_logs`
- 已将 `plan260422.txt` 中 P4 项目更新为完成状态
- 已将版本链路同步提升到 `v1.1.9`

### P4 当前边界

- P4 已完成“追踪识别偏离 -> 用户确认 -> 生成重排预案 -> 用户确认应用 -> 写入排程片段与审计日志”的主线闭环
- 当前匹配模型仍是规则型：任务绑定、关键词匹配、强偏离分类；后续可继续引入更智能的语义匹配或手动规则训练

### 2026-04-23 P5 安卓端任务 / 日程 / 追踪体验补齐

- 已依据 `plan260422.txt` 的 P5 主线完成安卓端体验补齐：
  - 追踪平台差异新增统一平台来源模型，明确 Windows 连续窗口采样与 Android Usage Stats 手动导入的不同能力边界
  - `activity_records` 新增 `device_id` 与 `platform` 存储能力，Windows 自动追踪和 Android 使用情况导入都会写入设备来源
  - 追踪页新增安卓专属状态卡片，说明“打开应用或手动刷新时读取系统使用记录，不常驻后台”，并提供权限入口和立即导入
  - 安卓追踪继续优先展示应用名，包名只作为底层技术标识保存
  - 安卓日报摘要隐藏键鼠输入专属指标，避免移动端展示不适用字段
  - 追踪页历史工具栏改为可换行布局，摘要卡片在手机宽度下转为单列，减少移动端溢出
  - 设置页新增“安卓端状态”，集中展示 Usage Access 权限、设备标识和 Outlook 移动端同步入口
  - Outlook 同步设置页增加移动端内边距和最大宽度约束，兼容手机和平板阅读
- 已将 `plan260422.txt` 中 P5 项目更新为完成状态
- 已将版本链路同步提升到 `v1.2.0`

### P5 当前边界

- Android 端当前仍按既定产品策略不做后台常驻追踪，只在打开应用或手动刷新时导入 Usage Stats
- P5 已完成基础适配与入口闭环，但正式发布前仍需要用户在真实手机和平板上验证布局、权限、提醒和同步状态显示

### 2026-04-23 v1.2.1 构建修复

- 已修复 P5 后用户构建反馈的编译错误：
  - `outlook_settings_page.dart` 的提醒服务导入路径修正为 `../reminders/reminder_service.dart`
  - `flowplan_archive_service.dart` 的日历 / 任务仓储导入路径修正为同级 feature 相对路径
  - 归档恢复中删除日程和删除任务的计数改为显式局部变量累加，避免 `num` 到 `int` 的推断错误
- 已将版本链路同步提升到 `v1.2.1`

### 2026-04-23 P6 Outlook 双向同步深化

- 已依据 `plan260422.txt` 的 P6 主线完成 Outlook 任务镜像深化：
  - 镜像绑定补齐远端快照、远端最后修改时间、冲突状态、冲突说明
  - 同步过程会先读取远端镜像状态，再判断是安全写回还是挂起冲突
  - 已支持识别四类关键状态：
    - 远端镜像已删除
    - 远端镜像已修改
    - 本地与远端都已修改
    - 最近一次远端写回失败
- 已补齐人工确认后的处理动作：
  - 以 FlowPlan 本地内容覆盖远端镜像
  - 采用 Outlook 镜像内容回填本地任务
  - 重建远端镜像
  - 解除镜像绑定但保留本地任务
- 已补齐批量维护能力：
  - 批量按本地内容覆盖远端镜像
  - 批量重建已被 Outlook 删除的远端镜像
- 已升级 Outlook 设置页：
  - 冲突候选面板会展示更多可执行操作
  - 逐条处理和批量处理入口都已接入
- 已升级 Outlook 诊断报告：
  - 新增远端删除、远端修改、双侧分叉、写回失败统计
- 已将用户确认触发的镜像处理动作写入数据操作日志
- 已将版本链路同步提升到 `v1.3.0`

### 2026-04-23 P6 ��β�ȶ����޸�

- �������� `plan260422.txt` �� P6 �����տ� Outlook ˫��ͬ����
- ���ؽ�������ͬ�����񡢾����ģ�͡��������ģ�ͺ���ϱ��������߼�������ǰһ�����ļ��ض��������Ⱦ�����Ĺ������ա�
- ���޸� Outlook ����ҳ�е��ֶμ���ͻչʾ����������״̬�İ���ȷ���û�ȷ�϶����Ľ���ɶ���
- �ѽ��汾��·ͬ�������� `v1.3.1`��
- ��ǰ�Խ�ֹ�ڱ��Ի��������κ� `dart`��`flutter`��������̬��������������û��ֶ���������Ϊ׼�����޸���

### 2026-04-24 5.1 ȫ�����ݲ���������

- �������� `plan260422.txt` ������ɡ�5.1 δ��ר�ȫ�����ݲ�����ơ���
- ��Ϊ�����ճ̡����񱾡��������Ĺؼ���ɾ�Ĳ�������ͳһ�����־��
- ��Ϊ `.ics` ���뵼�����ṹ���鵵���뵼�������ݿ⵼�������ݿ�ָ�׼�� / ȡ�� / ����Ӧ�ûָ���Outlook ͬ��ժҪ����ۺ���ơ�
- �ѱ����ų���䣺׷�ٲ��������������Usage Stats ����ȸ�Ƶ׷�ټ�¼�������������ݲ�����ơ�
- �ѽ��汾��·ͬ�������� `v1.3.2`��
### 2026-04-24 5.1 ȫ�����ݲ�������տ�

- �Ѱ������ճ̡����񱾡��������Ĺؼ���ɾ�Ĳ���ͳһ���� data_operation_logs
- �Ѱѽṹ���鵵��.ics ���뵼�����������ݿ⵼�������ݿ�ָ�׼����Ӧ�á�Outlook ͬ��ժҪ����ͳһ���
- ����ȷ�����ų��׷�ٲ��������������Usage Stats ���벻���������־�������Ƶ����д�����ݿ�
- ������Ӧ���ڡ����ݲ�����ơ���ѯҳ����Ϊ���ݿ�ֱ��֮����������ӻ����
