# FlowPlan 更新日志

当前版本：v1.4.0  
发布日期：2026-04-24

说明：
- 本文件记录从 `v1.0.0` 到当前版本的重要功能演进。
- 这里是面向产品层的版本摘要，不等同于每一次细粒度提交。

## v1.4.0

发布日期：2026-04-24

本次重点：
- 重构 Microsoft Outlook 个人账号连接链路，切换到 `Authorization Code Flow + PKCE`

更新内容：
- 新增统一的 Outlook OAuth 配置文件，集中管理 authority、authorize endpoint、token endpoint、redirect URI、scope、Graph 时区
- Outlook 登录固定改为 `https://login.microsoftonline.com/consumers`
- redirect URI 固定改为 `https://login.microsoftonline.com/common/oauth2/nativeclient`
- 登录改为生成并保存 `code_verifier`、`code_challenge`、`state`
- 设置页改为“连接 Outlook 日历 + 手动粘贴授权结果 + 提交授权码”的个人账号流程
- 新增授权会话校验，要求同一次登录的 `state` 才能完成换 token
- access token 过期后自动使用 refresh token 刷新
- 新增 `OutlookCalendarService`，补齐读取、创建、更新、删除 Outlook 日程的基础能力
- Graph 日历请求统一使用 `Asia/Tokyo` 时区头
- 强化 AADSTS50020、AADSTS50011、`invalid_grant`、`interaction_required`、`consent_required` 等错误提示

## v1.3.3

发布日期：2026-04-24

本次重点：
- 完成“全量数据操作审计”在应用内的闭环

更新内容：
- 将任务、日程、任务本、日历本、导入导出、数据库恢复、Outlook 同步等关键操作统一写入审计日志
- 新增应用内“数据操作审计”页面与设置入口
- 明确保留排除项：追踪采样、键鼠采样、Usage Stats 导入不进入审计日志

## v1.3.2

发布日期：2026-04-24

本次重点：
- 完成全量数据操作审计的后端链路收口

更新内容：
- 扩展 `data_operation_logs` 的覆盖范围
- 为导入导出、数据库恢复、Outlook 同步等高风险路径补齐审计记录

## v1.3.1

发布日期：2026-04-23

本次重点：
- 收口 P6 后的 Outlook 同步稳定性问题

更新内容：
- 修复 Outlook 设置页中的字段级冲突展示问题
- 修复同步结果文案与确认动作状态不一致的问题
- 修复若干 Outlook 同步相关构建与类型错误

## v1.3.0

发布日期：2026-04-23

本次重点：
- 完整完成 P6：Outlook 双向同步深化

更新内容：
- 重建 Outlook 任务镜像双向同步链路
- 新增本地任务与 Outlook 镜像之间的字段级双向同步策略
- 支持从 Outlook 镜像内容回填本地任务
- 支持手动处理冲突候选
- 支持批量重建被 Outlook 删除的远端镜像
- 升级 Outlook 设置页与诊断导出能力

## v1.2.1

发布日期：2026-04-23

本次重点：
- 构建修复与归档 / Outlook 导入路径修正

更新内容：
- 修复 Outlook 设置页提醒服务导入路径
- 修复结构化归档服务的仓储导入路径与默认类型解析
- 修复若干构建错误与类型错误

## v1.2.0

发布日期：2026-04-23

本次重点：
- 完成 P5：安卓端任务 / 日程 / 追踪体验补齐

更新内容：
- 明确 Android 仅支持 Android 16 及以上
- 为追踪链路补齐统一平台来源模型
- `activity_records` 增加 `device_id` 与 `platform`
- 安卓追踪页新增平台状态卡片、权限入口和手动导入口
- 安卓端优先展示应用名而不是包名
- 追踪页与 Outlook 设置页完成手机 / 平板基础布局适配

## v1.1.9

发布日期：2026-04-23

本次重点：
- 完成 P4：追踪与排期双向联动

更新内容：
- 建立“当前计划项”与“当前追踪项”的稳定可比较模型
- 支持根据活动记录识别当前是否在执行计划任务
- 支持识别计划偏离候选
- 偏离后可生成顺延重排预案，且保留人工确认边界
- 偏离提示与预案应用写入数据操作日志

## v1.1.8

发布日期：2026-04-23

本次重点：
- 完成 P3 的排程片段、人工确认边界与初版审计日志

更新内容：
- 新增 `task_schedule_segments` 数据模型
- 一键重排改为先生成预案，再由用户确认应用
- 可拆分任务支持多段排程片段持久化
- 时间轴支持按任务排程片段渲染
- 新增 `data_operation_logs` 初版数据操作日志表

## v1.1.7

发布日期：2026-04-23

本次重点：
- 完成 P3 排程引擎核心升级

更新内容：
- 支持多组工作时间
- 支持锁定任务避让
- 支持可拆分任务与连续任务的不同排程策略
- 提升一键重排结果的可解释性

## v1.1.6

发布日期：2026-04-23

本次重点：
- 完成 P2：提醒系统完整收口

更新内容：
- 运行时提醒服务按分钟扫描日程与任务
- 任务支持计划开始提醒、截止提醒、截止风险提醒
- 新增计划偏离提醒
- Android 接入系统级提醒调度与恢复机制
- Windows 补齐托盘通知与强提醒兜底

## v1.1.5

发布日期：2026-04-23

本次重点：
- 完成 P1：结构化导入导出与数据库恢复主线

更新内容：
- 完成 FlowPlan 自有结构化容器归档导入导出
- 支持按任务本 / 日历本选择性导入导出
- 支持数据库导出
- 支持数据库恢复准备、应用恢复、取消恢复
- 明确 `.ics` 与结构化归档的边界职责

## v1.1.4

发布日期：2026-04-23

本次重点：
- 完成 P0：Outlook 同步安全边界收口

更新内容：
- 明确只读 / 双向同步 / 暂停同步三态语义
- 保证普通 Outlook 日历默认只读
- 双向同步只允许写入 FlowPlan 托管容器
- 任务本可绑定 Outlook 专属镜像容器
- 升级 Outlook 设置页与同步诊断能力

## v1.1.3

发布日期：2026-04-23

本次重点：
- 完成 P2 第一轮提醒系统运行时闭环

更新内容：
- 新增运行时提醒服务
- 应用启动后定期扫描未来可提醒的日程与任务
- 日程读取默认提前提醒分钟数
- 任务支持基础开始提醒与截止提醒

## v1.0.0

发布日期：2026-04-22

起始版本说明：
- 建立 FlowPlan 的本地优先产品基础
- 形成时间轴、本周、月视图三大主界面框架
- 建立任务、日程、任务本、日历本的基础数据结构
- 建立 SQLite 数据库主链路
- 建立 Windows 桌面端基础壳与设置页框架
- 建立追踪页基础展示与后续主线规划入口
